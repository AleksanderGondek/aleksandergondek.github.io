import Link from "gatsby-link";
import * as React from "react";

const Header = () => (
    <div>
        <div>
            <h1>
                <Link to="/">Gatsby</Link>
            </h1>
        </div>
    </div>
);

export default Header;
